# Chief of Staff Brief — 2026-08-27

## Needs your decision this week, ranked

### 1. Debrief generation is probably still broken in production (Article II-adjacent: real money)
The 2026-08-27 fix split `generateDebrief()` into two Groq calls, each individually under the
8000 TPM free-tier ceiling. But TPM is a rolling 60-second budget across *all* calls, and the
two fire back-to-back with no gap. Planner's own arithmetic on the BACKLOG's measured token
counts: a typical 8-question interview needs ~14,300 tokens combined — call 1 succeeds, call 2
hits the remaining ~1,200-2,700 token budget and 429s. Retry re-runs both calls and fails again.
Unverified live (Groq egress-blocked in every sandbox this has been built in), but it's arithmetic
on the repo's own numbers, not a guess.
**Decision needed:** stay on Groq free tier (~6-8K TPM) and engineer around it (persist call 1's
result so retry only re-runs synthesis, gate call 2 on real remaining-budget headers, trim prompt
overhead), or move to Groq's paid Developer tier (~250-300K TPM, real spend — Article II) and make
this a non-issue outright. The engineering is worth doing regardless of which you pick.

### 2. Non-negotiable-rule violation: internal marker leaking into user-facing copy — fix now, no decision needed
`[FATAL FLAG] 40% of questions received zero-signal responses.` is being prepended directly onto
`overall_impression`, which renders as the interviewer's first-person verdict quote **and** ships
in the debrief email to real users. This is the exact "don't expose LLM internals in user-facing
copy" rule, and it's live. Small, self-contained fix (move the raw stat into the internal
`reasoning` column, rewrite the field in plain language) — routing to Coder now, this doesn't need
you to weigh in, just flagging that it existed.

### 3. PR #22 (PRD status → Google Sheets/Slack sync) — reopens a door you closed
Pulled the actual diff myself since Reviewer has no GitHub access. It's a GitHub Actions workflow
(cron + push-triggered) that would hold a standing Slack bot token and Google service-account key
as repo secrets — inert today (no secrets configured yet), but exactly the "automated seat holds
real external-system credentials" pattern `CLAUDE.md` says never happens, and it reopens the Slack
integration `docs/autonomy-charter.md` already recorded as deliberately closed (Enterprise-plan
only), just via a different mechanism.
**Decision needed:** merge as-is (reversing that earlier call), close it, or rework it to run only
when you manually trigger it rather than on a schedule.

## Also flagged, not urgent
- Legacy `round_type = "technical"` sessions get 5 questions from the question route but the
  debrief route still expects 8 — those sessions can never generate a debrief. Needs a read-only
  count of whether any production session is actually affected before deciding how to backfill.
- Resend client crashes `next build` in any environment missing `RESEND_API_KEY` (every other
  client in the repo is lazy-init; this one isn't) — CI doesn't run `next build` so this is invisible
  until it isn't.
- Branch protection on `main` still unverified — no MCP tool covers it, needs you in GitHub
  Settings → Branches directly.
- Five features (drill loop, cross-session trends, `/api/preview-analysis`, `/dev/quick-test`,
  new Mixpanel funnel events) are built but never verified against live keys — same two sandbox
  blockers (Groq egress, Clerk SDK) each time. Needs one real end-to-end pass with working keys.
- Founder photo on the homepage is still a placeholder gradient block.
- Mobile responsiveness is still only code-analysis-verified, never checked in a real browser
  against live content — the one remaining unverified Week-1 "Must Have."

## Org status
Phase 0 confirmed live and working (two-seat smoke test passed this session). Tech pod is
active — Planner just did its first real scoping pass above. Department heads (Legal, Analytics,
Product, Marketing, Sales, Monetization) still haven't produced their Phase 0 first-pass work or
hiring specs — `ops/<department>/` folders are still empty.
